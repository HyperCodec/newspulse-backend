[project]
name = "steelhacks-xiii"
version = "0.1.0"
description = "Ingestion and DB tools"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "surrealdb>=2.0.0",
]

[tool.uv]
package = false